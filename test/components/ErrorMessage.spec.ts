import { describe, test } from 'vitest'

describe('ErrorMessage', () => {
  test('should render good HTML')
  test('should indicate 404 message')
  test('should render 403 message')
  test('should render "An error occured." when status code error is not catch')
})
