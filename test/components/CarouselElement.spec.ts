import { describe, test } from 'vitest'

describe('CarouselElement', () => {
  test('should render good HTML')
  test('should render navigation when items are greather than 1')
  test('should hide navigation when items are less or egal 1')
})
